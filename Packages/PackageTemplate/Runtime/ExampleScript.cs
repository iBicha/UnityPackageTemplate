using UnityEngine;

namespace Company.Product
{
    public class ExampleScript : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("Company.Product.ExampleScript.Start");
        }
    }
}